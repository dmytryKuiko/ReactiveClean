package com.example.dimi.reactiveclean.models.content

class ContentNavigation(val currentPage: Int,
                        val pageSize: Int,
                        val pages: Int,
                        val startIndex: Int) {
}